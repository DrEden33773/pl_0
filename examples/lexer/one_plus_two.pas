program OnePlusTwo;
begin
  var a, b, c;
  a := 1;
  b := 2;
  c := a + b;
  write(c);
  var boolean := a = b;
  boolean := a < b;
  boolean := a > b; 
  boolean := a <= b;
  boolean := a >= b;
  boolean := a <> b;
  write(boolean);
end